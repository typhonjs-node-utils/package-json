import test             from '../util/test.js';

if (test.categories.getPackagePath)
{
   /**
    * TODO: Add passing / positive tests for getPackagePath
    */
   describe(`getPackagePath`, () =>
   {
   });
}
